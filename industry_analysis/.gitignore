# Local-only working notes. Stays on this machine, never pushed.
misc/

# Generated reports are run artefacts, not source. Remove this line if you would
# rather keep them in version control.
output/*.md
